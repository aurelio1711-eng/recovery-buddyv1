name: Deploy to Vercel

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          cache: npm

      - run: npm ci --ignore-scripts

      - run: npm test -- --run

      - run: npx vercel pull --yes --project=prj_FiuOrVDrjYzNt6we9ZiOAjmjua54 --scope=team_kQeZbOpy6wgFTqfr9FV8sHUM --token=${{ secrets.VERCEL_TOKEN }}

      - run: npx vercel build --project=prj_FiuOrVDrjYzNt6we9ZiOAjmjua54 --scope=team_kQeZbOpy6wgFTqfr9FV8sHUM ${{ github.ref == 'refs/heads/main' && '--prod' || '' }} --token=${{ secrets.VERCEL_TOKEN }}

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        run: |
          npx vercel deploy \
            --prebuilt \
            --project=prj_FiuOrVDrjYzNt6we9ZiOAjmjua54 \
            --scope=team_kQeZbOpy6wgFTqfr9FV8sHUM \
            --token=$VERCEL_TOKEN \
            ${{ github.ref == 'refs/heads/main' && '--prod' || '' }} \
            --yes
